# PYEBWA Token Deployment

## Build

- `cd pyebwa-token/mobile`
- `npm install`
- `npx expo start`
- `npx expo run:android`
- `npx expo run:ios`

## Notes

- The mobile package now uses the local/API-backed service layer in this repo.
- There are no remaining legacy mobile backend setup requirements in this deployment flow.
